import { Header } from '../types';

const header: Header = {
    logo: 'san.',
    label: 'Header',
    links: [
        { label: 'Home', url: '/' },
        { label: '/uses', url: '/uses' },
        { label: 'Contact', url: '/contact' },
    ],
};

export default header;
