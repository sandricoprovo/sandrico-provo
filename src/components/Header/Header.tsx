import styled from 'styled-components';

const HeaderStyled = styled.header``;

export function Header() {
    return (
        <HeaderStyled>
            <p>Header</p>
        </HeaderStyled>
    );
}
